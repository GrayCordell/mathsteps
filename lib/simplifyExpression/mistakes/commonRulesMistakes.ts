// import { commonPoolOfRules } from '~/simplifyExpression/rules/commonRules.js'
// import { SIMPLIFY_ARITHMETIC__ADD } from '~/ChangeTypes'
//
/// / Utilizes this find util to get the rule we are making a mistake from
// const find = (id: string, l: string) => commonPoolOfRules.find(rule => rule.id === id && rule.l === l)
// const poolOfMistakeRulesMap = new Map()
//
/// / l: find(SIMPLIFY_ARITHMETIC__ADD, 'n1 + n2').l, // 'n1 + n2',
/// /  mistakes: [
/// /  { getFromRule: `${SIMPLIFY_ARITHMETIC__MULTIPLY}_n1 * n2` },
/// /  { getFromRule: `${SIMPLIFY_ARITHMETIC__SUBTRACT}_n1 - n2` },
/// / ],
